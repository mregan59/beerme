const initialState = {
    loading: false,
    beers: [],
};

export function adminReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
